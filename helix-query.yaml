version: 1

indices:
  firsteds:
    include:
      - '**'
    exclude:
      - '**/blocks/**'
    target: /query-index
    properties:
      title:
        select: head > title
        value: text(el)
      tag:
        select: head > meta[name="tag"]
        value: attribute(el, 'content')
